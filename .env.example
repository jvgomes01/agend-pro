# ==============================================
# VARIÁVEIS DE AMBIENTE - AgendPro
# ==============================================
# 
# Este arquivo serve como EXEMPLO das variáveis necessárias.
# 
# IMPORTANTE: 
# 1. Copie este arquivo para .env
# 2. Substitua os valores de exemplo por valores reais
# 3. NUNCA commite o arquivo .env no Git
# 
# ==============================================

# ----- DATABASE -----
# URL de conexão com o banco de dados
# Para desenvolvimento, SQLite é suficiente
DATABASE_URL="file:./dev.db"

# Para produção, use PostgreSQL:
# DATABASE_URL="postgresql://usuario:senha@host:porta/database?schema=public"

# ----- NEXTAUTH -----
# URL base da aplicação
NEXTAUTH_URL="http://localhost:3000"

# Secret para criptografia de tokens
# IMPORTANTE: Gere um secret único e aleatório para produção!
# Você pode usar: openssl rand -base64 32
NEXTAUTH_SECRET="seu-secret-aqui-mude-em-producao"

# ----- OPCIONAL -----
# Modo de desenvolvimento (mostra logs detalhados)
NODE_ENV="development"

# Porta da aplicação (padrão: 3000)
# PORT=3000

# ==============================================
# NOTAS DE SEGURANÇA
# ==============================================
#
# 1. NEXTAUTH_SECRET deve ser diferente em cada ambiente
# 2. Nunca use valores de exemplo em produção
# 3. Mantenha credenciais de banco seguras
# 4. Use variáveis de ambiente do servidor em produção (Vercel, etc)
#
